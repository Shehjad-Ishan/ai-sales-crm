# Copy me to .env if you want to override defaults
LLM_PROVIDER=ollama
OLLAMA_MODEL=gemma3

# For GROQ fallback instead of Ollama
# LLM_PROVIDER=groq
# GROQ_API_KEY=sk_your_key
# GROQ_MODEL=llama-3.1-8b-instant

# MailHog by default (for real SMTP use provider host/port + creds)
SMTP_HOST=mailhog
SMTP_PORT=1025
FROM_NAME=Acme SDR
FROM_EMAIL=sdr@acme.test

LEADS_CSV=/data/leads.csv
OUTPUT_CSV=/data/leads_out.csv
GENERATE_FAKE_REPLIES=true
MAX_LEADS=0
